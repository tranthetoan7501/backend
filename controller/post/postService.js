const User = require('../../model/user');
const Post = require('../../model/post');

// exports.createPost = (data) => {
//   data.body.owner = { id: data.user.id, username: data.user.username };
//   const post =
//   return post;
// };
